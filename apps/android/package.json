{
  "name": "@service-monitor/android",
  "version": "0.0.1",
  "private": true,
  "description": "Android shell stub — shared contract on @service-monitor/core/browser",
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-strip-types src/contract.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@service-monitor/core": "*"
  },
  "devDependencies": {
    "@types/node": "22.15.30",
    "typescript": "5.8.3"
  }
}
